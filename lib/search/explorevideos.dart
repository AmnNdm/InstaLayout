import 'package:flutter/material.dart';

class ExploreVideos extends StatelessWidget {
  const ExploreVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
